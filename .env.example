# Anthropic API key (required for RAG synthesis and Claude-based LLM backend)
ANTHROPIC_API_KEY=

# OpenAI API key (required only if using OpenAI backend)
API_KEY=

# HuggingFace token (required only if using HF Inference Router backend)
HF_TOKEN=

# Milvus vector DB URI (required for RAG pipeline)
MILVUS_URI=http://localhost:19530
